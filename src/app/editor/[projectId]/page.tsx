import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Editor',
};

const EditorPage = () => {
  return (
    <div>
      <h1>Editor Page</h1>
    </div>
  );
};

export default EditorPage;
